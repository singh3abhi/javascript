/**
 * Coercion
 * Whenever we do an operation based on the input, we can actually convert the type of input
 * for operation
 *
 * Explicit TypeCasting -> The conversion can be manually done by us
 *                  OR
 * Implicit TypeCasting -> The language based on some certain rules
 * automatically converts the types
 *
 * Implicit TypeCasting is also known as Coercion
 */
